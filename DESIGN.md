# 拼车工作台设计系统

## 方向与边界

用户于 2026-09-14 选定 **B「信号控制室」的布局 + Claude 风格的配色与样式**。这项明确选择替代先前的暗色横向导航，以及过程中的 A 冷白蓝色提案。

借用的是暖纸色、陶土强调、安静的阅读节奏与柔和控件，不使用 Claude 名称、标识、专有字体或聊天界面，也不把管理工具改成营销页。

本轮不创建 Trellis 任务。按全局骨架、管理页面、用户页面和浏览器验收分步推进；其他工作流正在修改的请求明细与数据保留业务逻辑必须保留。

## 产品与功能契约

面向拼车管理员与乘客。管理员需要管理用户、车辆、成员、上游账号，查询用量、请求与审计，查看价格状态并安全清理明细。乘客需要判断个人额度、同车用量和账号状态，管理自己的 API Key。

- 三秒内回答「还剩多少 USD、何时重置、是否有未知或不完整数据」。剩余额度不是账户余额，不新增充值、支付或汇率。
- 个人金额配额与 CPA 上游账号原生配额是独立概念，缺失显示「上游未提供」，不得伪造 0%。
- 同车成员用量保持公开；费用未知不显示为已知零；超额仍显示真实超额数值。
- 保留强制改密、会话失效、CSRF、角色导航、API Key 一次性展示和清理预览确认。
- 管理端所有页面、用户端所有页面、登录、改密、表单、详情及异常状态统一换用新表现层；API、安全与记账契约不变。

## 设计读取与拨盘

这是有阅读温度的运营工作台，不是冷硬监控大屏。保留 B 的空间关系与数据扫读效率，把硬线、深底与紧张感替换成暖色表面与更温和的分组。

视觉冒险度 6 / 动效强度 3 / 信息密度 6。动效只解释当前交互，不自动轮播、不闪烁、不延迟主要任务。

## 参考转译

从本地 `qiaomu-design` 的 Claude 设计资料只提取以下具体规则：

1. `#f5f4ed` 暖纸底色和 `#faf9f5` 象牙表面，而不是冷蓝灰。
2. `#c96442` 陶土角色；带白字的小按钮用更深的 `#a65034` 以提高文字对比度。
3. `#e8e6dc` 暖色交互底与弱边界；不使用左侧装饰色条。
4. 控件约 12px、工作区约 18px 圆角，短单层暖阴影，只用于真实层级。
5. 系统中文正文搭配少量衬线金额读数，行高 1.5–1.65；不加载专有或网络字体。

## 视觉令牌

| 角色 | 方向值 |
| --- | --- |
| 页面背景 | `#f5f4ed` |
| 工作区表面 | `#faf9f5` / `#ffffff` |
| 交互底色 | `#e8e6dc` |
| 主文字 | `#292824` |
| 次文字 | `#66645e` |
| 弱边框 | `#e2dfd5` |
| 主动作 | 深陶土 `#a65034`，白字 |
| 次强调 | 陶土 `#c96442`，不能代替所有状态色 |
| 健康 | 低饱和深绿色，必须伴随文字 |
| 警告 / 错误 | 深赭色 / 暖深红，浅色背景与具体下一步 |

具体实现的 token 在 `app.css` 中集中维护。背景、文字、线条和状态分开命名，禁止在业务 JavaScript 中散落样式。

## 排版与层级

- 中文正文、按钮、导航使用系统字体，14px 起，字重 400/500/600；禁止斜体和负中文字距。
- 页面标题约 24–28px，不使用营销型大标题；大金额使用 Georgia/本地衬线后备，表格数字为等宽或表格数字。
- 同一阅读区只有一个主要读数；「已用、上限、重置、未知费用」围绕剩余值展开，不把全部指标变成等重的大卡片。
- 代码和公开引用允许换行；API Key 明文只在一次性展示区出现，不加入截图或日志。

## 布局与导航

### 全局骨架

- 桌面采用约 88px 窄轨导航，图标与短中文标签并存。导航占满视口并独立滚动；主工作区固定标题栏、内容区独立滚动，使用 `min-height:0` 和 `overscroll-behavior:contain`。
- 顶部只保留产品、当前页上下文、用户和退出；不再把全部管理入口塞到横向顶栏。
- 路由使用原生锚点，保留原有 hash 与请求筛选参数处理；同一轮异步渲染只更新自己的内容节点，旧请求不应覆盖新页面。
- 手机将导航折叠到明确的菜单按钮，展开后可完整访问全部入口。隐藏导航不能残留可聚焦控件；菜单关闭后保持焦点路径可用。

### 管理端

- 概览：真实数量状态带、车辆入口和高频动作，不生成假趋势图或伪造异常数量。
- 账号管理：入口按「车辆管理 → 账号管理 → 用户管理」排列，只展示 OpenAI/Codex；列表保留可辨识文件名与状态，手机优先显示账号、状态和操作，提供商与时间可在详情查看，不能要求横向滚动才能启停。
- JSON 导入逐项显示结果，只接收含 `access_token` 的 `type: codex` 授权文件；API Key 沿用原版管理配置。同名文件拒绝覆盖并提示重命名。OAuth 明确说明手工回填完整回调地址，关闭与离页立即停止轮询。
- 账号启停成功后重新读取列表；并发刷新必须丢弃过期快照并补发刷新，不能让旧响应覆盖已成功的操作。
- 用户和车辆：左侧紧凑列表、右侧非模态详情面板。保留完整编辑能力；窄屏转为单列，管理详情不能被列表遮住。
- 详情使用原生非模态 `dialog` 的关闭能力；创建、一次性秘密与独立调额使用原生模态弹层，不把复杂操作拆成嵌套模态。
- 报表、请求：筛选和结果分区，明细保留展开与重试；金额、事件数与未知状态可扫读。
- 价格、审计和数据保留：按阅读、记录、危险操作分别组织，不照搬额度卡片；清理确认有明确范围与后果。

### 用户端

- 概览以剩余额度为主要读数，个人额度与车辆事实分区；API Key、成员与账号状态有直接入口。
- 成员用量保留金额与请求统计；账号窗口区分已观测、陈旧、缺失、状态-only。
- Key 管理、改密和强制改密保持简洁任务流；退出或关闭时清除一次性秘密。

### 登录

采用安静的产品说明区与独立登录表单，不展示虚构实时状态或伪造统计。窄屏首先保证登录字段与提交可用。

## 状态和交互

- 控件覆盖 hover、active、focus-visible、disabled、loading；主要按钮最小 44px 触摸目标。
- 页面首次加载与已有数据刷新区别处理；错误包含重试，不把失败呈现为空结果。
- 使用原生 `progress` 的 `value/max` 表达比例，严格 CSP 下不添加内联样式，也不放宽安全策略。
- 弹层有名称、明确关闭动作与焦点返回；非模态详情不阻断列表切换，独立编辑有焦点约束。
- 动效 140–200ms，为操作反馈服务；提供 `prefers-reduced-motion`，禁止 `transition:all`。

## 响应式与验收

目标桌面 1440px、窄桌面 1024px、手机 390px / 360px。表格可自身横向滚动，但页面不能整体溢出。移动端使用单一主滚动区，长文本、长模型名、未知费用和失败说明保持可读。

验收使用隔离数据库和合成凭据：管理端九个导航入口、乘客五个导航入口、登录、强制改密、创建与分配、调额、Key 管理、报表、原生窗口、请求展开和保留确认。任何未执行的检查必须明确标记，不把设计规范或样机当作通过证据。

## 反目标

不使用深色默认主题、冷蓝主色、霓虹、毛玻璃、渐变标题、左侧强调竖线、无语义卡片墙、假实时数据、装饰性图表或 Claude 品牌资产。继续保持 Go 内嵌静态资源与原生 JavaScript，不引入 Node 前端构建或新框架。
