# Carpool 额度工作台设计锚

## 产品与受众

这是 CPA 拼车模块的管理员与乘客工作台，服务于日常额度判断、同车成员核对、上游账号窗口查看、请求费用排查和数据清理。乘客和管理员都必须在三秒内看见“是否还能请求、还剩多少美元、何时重置、统计是否完整”。

## 选定方向

用户于 2026-09-05 选择方向 B「暗色仪表台」，并提出“更圆润、偏向 OpenAI 一点”。这里借鉴的是克制的中性深色、柔和圆角、宽松留白和明确状态层级，不复制 OpenAI 标识、文案或品牌组件。

目标气质是安静、可靠的夜间监测席位：数据密集但不压迫，异常醒目但不刺眼，圆角只用于表达容器和可操作控件，不能把页面变成无语义卡片墙。

## 功能契约

- 改密门禁必须解释“管理员已重置密码、业务数据仍在、设置新密码后重新登录”。
- 乘客首屏显示本期 USD 限额、已确认费用、剩余/超额、账期、未知费用数量和覆盖起点；同车成员金额公开。
- 账号显示 CPA 原生配额窗口；缺失显示“上游未提供”，陈旧显示“数据陈旧”，不得画成 0%。
- 管理员可完成上车分配、即时调额、请求明细筛选/展开、价格状态查看、保留设置、预览清理和确认作业。

## 视觉系统

### 色彩角色

- 页面底色：`#0f1418`，局部工作区：`#151c21`，浮层：`#1c262c`。
- 主文字：`#edf4f3`，次文字：`#9aadb0`，弱线：`#2f4148`。
- 主操作与健康状态：`#73d2bf`；信息提示：`#8fc5e0`；警告：`#e4b56d`；阻止/错误：`#e99468`。
- 颜色只表达状态和动作，不能用渐变、霓虹 glow 或随机彩色装饰。

### 排版

- 中文正文和控件使用 `system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", "Noto Sans SC", sans-serif`。
- 金额、百分比、时间和请求 ID 使用 `ui-monospace, SFMono-Regular, Menlo, Consolas, "Noto Sans Mono CJK SC", monospace`，启用 `font-variant-numeric: tabular-nums`。
- 标题保持短句，避免超大 Hero；正文行高 1.5 左右，数字与标签之间留出可扫描的间距。
- 不使用斜体、渐变字或装饰性网络字体。

### 形状与深度

- 页面和工作区使用 18px 至 24px 圆角；输入、按钮和状态胶囊使用 10px 至 12px 圆角；表格行不套独立圆角卡片。
- 采用 1px 弱边框和单层短阴影区分浮层；不使用玻璃拟态、内发光或多层阴影。
- 进度槽是连续的圆角条，真实百分比文本单独显示；超过 100% 时槽填满并保留超额数值。

## 布局与组件

- 顶部使用紧凑的状态栏和横向主导航；不保留旧 232px 深侧栏。
- 管理员页面优先采用“状态带 → 当前额度主读数 → 成员/账号监测区 → 请求记录”的纵向阅读路径；宽屏可用不等宽双列，窄屏改为单列。
- 乘客页面把自己的额度读数置于首屏中央，同车成员用紧凑列表呈现；账号窗口使用可键盘操作的 `details/summary` 展开其余窗口。
- 请求明细用一行一个逻辑请求，展开后查看 CPA 上游事件；主行金额右对齐，未知费用不能显示为 `$0.00`。
- 危险清理操作采用页面内确认区或抽屉式确认，不嵌套弹窗；确认前显示冻结范围、预估数量和在途请求数。
- 所有控件提供 hover、active、focus-visible、loading、empty、error、success 状态；动画只做 140–220ms 的状态反馈，并尊重 `prefers-reduced-motion`。

## 关键动作

签名动作是“分段额度扫描”：用户先读当前金额主读数，再沿同一条分段槽查看已用、剩余、重置和完整性状态。管理员调额后读数和成员状态即时更新，不需要刷新页面才能判断结果。

## 响应式与可访问性

- 桌面目标宽度 1440px，内容区保持 24px 至 40px 外边距；移动目标 390px 和 360px，不产生整页水平溢出。
- 表格在窄屏使用自身横向滚动或摘要行，不能压缩到不可读；金额和错误说明允许换行。
- 键盘焦点始终可见，进度条保留原生 `role="progressbar"` 和文本百分比；状态不能只靠颜色传达。
- 200% 放大、空数据、长模型名、费用未知和作业失败均有可读布局。

## 反目标与边界

- 不恢复旧配色、旧导航骨架、四统计卡或营销 Hero。
- 不把乘客限额误当 CPA 上游账号原生配额；不对缺失配额或未知费用绘制假数据。
- 不新增充值、汇率、图片媒体或超出既有路由白名单的功能。
- 生产实现继续使用 Go 内嵌资源与原生 JavaScript；本文件只约束表现层，不改变业务 API、安全和账务契约。

## 视觉验收

实现后使用隔离数据库检查管理员和乘客的桌面/移动流程：三秒内能辨认剩余金额、重置时间和异常状态；深色页面在无颜色时仍能通过布局和字体区分区域；圆角和留白提升可读性而没有卡片海；强制改密、未知费用、上游缺失、超额和清理确认均显示具体下一步。
