# 明细与保留实施计划

## 2026-09-14 收尾范围（用户同意复用任务并开始）

本轮先核对已实现功能与测试证据，补请求明细筛选/稳定分页、保留设置重启与回退、
清理预览及重复确认的真实缺口；只做业务正确性修复，不做前端重设计。
先形成代码与验收映射，涉及公开接口或破坏性操作语义的变更先写清最小契约再实施。
保留当前计费冻结、同步记账及 in_progress/incomplete 保护。旧设计的租约、
全局封锁、复杂崩溃恢复、持久化 outbox 不属于本轮，不为满足旧文档而恢复这些体系。
不触碰未提交的 design-previews；不操作生产数据库，不调用真实上游，不推送或部署。


## 启动条件

父任务已启动，`09-05-passenger-billing` 已通过账本/计价检查并交付字段契约。
完整读取父设计第 3.3、6 至 9 节、本子任务文档与 JSONL；没有独立金额汇总不得开启删除。

## 顺序

- [ ] 先写一请求多事件及复合筛选/稳定游标测试，再实现 repository/service/API。
- [ ] 补齐安全历史快照和明细展开，价格状态、账期历史与已有聚合视图联通。
- [ ] 添加设置/作业递增迁移，默认、永久、配置回退、重启持久化与改动即时生效。
- [ ] 自动清理改用有效设置及已结算判断，证明所有金额/未知累计保持。
- [ ] 三种独立作业的预览、范围绑定、确认幂等、修订冲突、有界批处理与审计。
- [ ] 完成已结束期删除与本期重置保护，覆盖在途请求、迟到事件和批次中断恢复。
- [ ] 管理页面完整状态：筛选 URL、加载更多、展开、危险提示、二次确认、作业结果及重试。
- [ ] 记录与设置页面按所选 `DESIGN.md` 验证新布局和完整导航，使用 B 方向的圆润暗色监测语言，
  禁止旧样式回流。
- [ ] 运行定向/安全/浏览器验证，`trellis-check` 通过后交回父任务做全量验收。

## 定向验证

```bash
gofmt -w .
go test ./internal/carpool/... ./internal/config
go test -race ./internal/carpool/...
node --check internal/carpool/web/assets/app.js
go build -o test-output ./cmd/server
```

遵守父计划的输出路径安全规则。数据库测试包括：同一 started_at 的多行稳定游标、
新记录插入、模型同时匹配请求/事件、一个请求上千事件的有界删除、当前期被清理但仍拒绝
新请求、删除历史期后旧明细仍可读、重置后重复 event 不恢复旧费用、跨月预览失效。
浏览器完成筛选/展开/回退恢复与三种操作的预览、取消、确认和失败；只使用测试数据。

## 回滚点

先完成账本独立性和无在途证明，再接删除入口；任何累计变化回归未解决不能上线清理。
UI 出错不自动重发危险操作，通过服务端确认引用查询已完成结果。清理数据只能从备份恢复，
恢复不是自动重试的一部分。

## 2026-09-14 收尾证据映射

- [x] 后端组合筛选、固定25条/跨午夜稳定游标、实际period与安全Token DTO。
- [x] 既有页面真正加载事件、未知小计、筛选恢复，不做布局重设计。
- [x] 保留设置真实重启与配置回退；读取失败不执行自动删除，批间新策略生效。
- [x] 绑定预览、同ID重复确认、事务审计/结果回滚、跨期冲突、1000+1续批。
- [x] 新增与原有两套真实浏览器流程及独立检查；全量测试/vet/双构建通过。
- [x] 用户已确认本轮两批本地提交；提交记录见会话日志。
- [ ] 暂缓的完整前端验收项与父任务最终整体验收；不自动归档。

本轮有效边界与命令/截图见 `research/closure-acceptance.md`。旧条目包含的全面重设计、
逐事件恢复、自动跨进程续跑不借本轮验收声称已完成。
