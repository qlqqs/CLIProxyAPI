# 实施计划与验收门禁

## 状态

用户已明确批准最终方案与默认容量 10，任务已进入 in_progress。核心、API/UI、独立检查与浏览器/真实 HTTP 验收全部完成，证据见 `research/final-verification.md`；用户已确认提交；全部计划文件已在现有提交 `3b1910ce` 中，随后归档并记录会话。

## 顺序与职责

### 1. 实施前确认

- [x] 检查工作区改动和任务状态，加载 trellis-before-dev 与相关 backend 规范。
- [x] 确认最新批准内容覆盖队列默认容量、单实例边界、周期待同步和账号更换规则。
- [x] 对照 `design.md` 梳理 Store 接口、授权路径、所有 membership/user/account 扫描、DTO、配置默认值、生命周期和清理消费者。
- [x] UI 读取项目 `DESIGN.md` 与 qiaomu-design 功能界面参考，不重新选视觉方向。

### 2. 核心实现

按 Trellis 默认调度 `trellis-implement`。派发以 `Active task: <task.py current 路径>` 开始，优先原生注入、缺失时让子代理读取 implement.jsonl。主会话协调契约与验收，不与子代理同时编辑其写集。

- [x] 新 migration、nullable 配额/并发策略、独立短周期账本和请求费用归属；同步去重和清理保护一并实现。
- [x] 提供窗口规范化与幂等观测，覆盖首次缺失、过期缺失、重复/乱序观测、边界修正、启用覆盖与长请求归属。
- [x] 实现双维原子计数、有界等待队列、取消/关停、就绪顺序、调额唤醒；不加网络 timeout 或外部组件。
- [x] 串联前置校验、排队、出队复核与一次性最终请求记录/快照；记录拒绝原因。
- [x] 覆盖普通响应、流、重试、无效正文和取消的幂等释放；SDK 如需变更仅增加可选服务器钩子，不影响旧 Key。

核心批次主要拥有：`internal/carpool/domain/`、`store/`、`runtime/`、`accounting/`、`service/`、`module.go`、配置及必要 SDK 生命周期扩展，连同相邻测试。由于 HTTP 接线与 service 强相关，具体交界在派发前明确，禁止多个代理争抢同一文件。

### 3. API 与界面集成

在核心契约稳定后继续实施，不与上批争抢共享文件。主会话负责界面设计协调，可将有明确写集的功能补丁交给实现子代理。

- [x] 成员/账号配置 API、缺省/null/零/正整数语义及安全审计；更新 DTO 与旧接口兼容测试。
- [x] 接入 HTTP 队满 429、writer 503、额度拒绝及上下文取消；真实路由验证不产生意外上游调用。
- [x] 现有成员调额表单、账号表单、乘客额度/并发展示；月重置说明、待同步与统计覆盖文案。
- [x] 更新 config.example.yaml、相关测试 fixture；不编辑真实运行配置。

### 4. 独立检查与修复

代码完成后派发 `trellis-check`，注入 check.jsonl；要求实际跑测试并自修确定缺陷。主会话复核高风险的费用清零、重复结算、取消泄漏、重新授权及跨 Key 隔离，不重复无关研究。

- [x] 单元：每个新字段边界值；两个有效周期分别达额；null 不限和零额度；金额溢出；已知/未知费用。
- [x] 周期：同车同 reset、重复/乱序/修正不清零、首次信号请求归属、跨周期长流、未同步费用后归属、重启/清理、账号更换历史保留。
- [x] 队列：默认容量 10、10 个等待者占满时第 11 个需等待的请求返回 429、可配置容量边界、双条件同时取得、同用户不同 Key、同账号不同用户、无头部阻塞、取消与授予竞态、热调额度/并发、关停唤醒。
- [x] 重新授权：排队中用户禁用、Key 撤销/到期、成员换车、账号更换、费用用尽、writer 故障，出队不得执行。
- [x] 生命周期：普通、SSE、上游失败、客户端取消、无效 JSON、空结果、内部重试；最终活动数/队列长度均回落，无重复释放。
- [x] 数据：迁移 checksum 与旧数据升级；费用事件去重；普通清理不重置额度；月本期重置仍只影响月账期。
- [x] 兼容：旧 Key、模型查询、Home 拒绝、不支持路由、账号原生额度投影保持原语义。

### 5. 验证命令与浏览器

```bash
gofmt -w .
go test ./internal/carpool/... ./internal/config/... ./sdk/access/...
go test -race ./internal/carpool/...
go test ./...
go vet ./...
go build -o test-output ./cmd/server && rm test-output
node --check internal/carpool/web/assets/app.js
```

如改动 SDK 执行/HTTP 生命周期，对实际受影响的 `sdk/api/handlers/...`、`sdk/cliproxy/...` 增补定向与 race 验证。禁止用 wall-clock Sleep 证明队列顺序/周期重置；使用注入时钟、channel 屏障和临时 SQLite。

- [x] 使用隔离 browser fixture 与假账号，实际编辑额度和并发、发起请求并观察入队/完成、核对月/周/5h 数据。
- [x] 桌面与移动浏览器操作、截图、控制台检查；待同步、超额、保存失败和长数值状态可读。
- [x] 将命令结果、失败原因与浏览器证据写入 task research；未执行不能记为通过。

### 6. 收尾与回滚点

- [x] 最终全范围检查通过，更新 backend 拼车、同步记账与清理规范，保留中英文语言约束。
- [x] 按 trellis-finish-work 处理提交/会话记录；不自动归档其他尚未完成任务。
- 回滚点 A：只有规划文档，尚无运行行为变化。
- 回滚点 B：隔离库验证失败，停止实施修正 migration，不动生产库。
- 回滚点 C：实现发现需突破单账号/单实例或新增复杂恢复协议，退回规划，重新取得范围批准。
