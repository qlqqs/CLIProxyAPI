# 实施计划

## 阶段 1：存储与领域合同

- [ ] 盘点现有月账期和短周期表的实际唯一键、归属与迁移路径，确定复用或新增本地窗口表。
- [ ] 新增递增 SQLite migration；使用工作区下一个空闲版本号，避开并行新增的 `005_api_key_tokens.sql`；保留旧月字段和历史表，不修改已有迁移。
- [ ] 将成员 5h/7d 限额统一为 `0 = 不限`，补充本地窗口状态与确定性时钟测试。
- [ ] 移除活跃领域/仓储接口中的月额度写入、月账期创建与月准入依赖。

## 阶段 2：准入与记账

- [ ] 准入只检查 5h/7d；出队复核同样只检查这两项及现有并发/授权/记账健康。
- [ ] 可靠费用事件原子且幂等地累计到两个本地窗口，窗口到期后由下一笔费用重建。
- [ ] 保持最后一笔可超额、后续拒绝；覆盖 0 不限、边界等额、调高/调低、窗口到期和未知费用。
- [ ] 删除月累计对当前请求的影响，同时确保历史请求金额、事件收据与去重不丢失。

## 阶段 3：HTTP 与管理操作

- [ ] 创建成员 DTO 改为 5h/7d；额度 PATCH 删除月字段并保持独立部分更新。
- [ ] 当前乘客与成员用量 DTO 仅输出 5h/7d 本地窗口状态。
- [ ] 删除“重置本月用量”路由/操作，增加同时重置 5h/7d 用量的受确认管理操作与审计。
- [ ] 更新稳定错误、响应与 API 流程测试；旧月字段不能重新启用月逻辑。

## 阶段 4：前端

- [ ] 加入成员表单仅显示 5h、7d，默认值 0 并明确 0 表示不限。
- [ ] 调额弹层仅显示 5h、7d、用户并发。
- [ ] 乘客概览主读数改为 7 天剩余额度，覆盖不限、未开始、正常、已用尽、超额和重置时间。
- [ ] 成员用量只显示 5h/7d，并保留独立并发列。
- [ ] 数据保留页替换为 5h/7d 重置确认；全局清理有效月额度文案。
- [ ] 更新 `DESIGN.md` 中三秒功能契约与额度结构。

## 阶段 5：验证与审查

- [ ] `gofmt -w .`
- [ ] `node --check internal/carpool/web/assets/app.js`
- [ ] 运行受影响 Node 测试与全部 Carpool Go 测试。
- [ ] `go vet ./...`
- [ ] `go test ./...`
- [ ] `go test -race ./internal/carpool/...`
- [ ] `go build -o test-output ./cmd/server && rm test-output`
- [ ] `CGO_ENABLED=0 go build -o test-output ./cmd/server && rm test-output`
- [ ] 使用隔离旧 SQLite 副本验证升级、5h/7d 累计、窗口到期、重置与重启恢复。
- [ ] 浏览器验收桌面 1440px、移动 390px/360px：概览、成员用量、创建/调额、重置确认，无整页横向溢出。
- [ ] 独立 `trellis-check` 核对 Sub2API 对标、跨层数据流、旧月逻辑残留和上一轮并发列改动。

## 并行工作区保护

- 当前除上一轮并发列外，工作区还出现 API Key token 持久化相关的并行未提交修改与 `005_api_key_tokens.sql`；实施代理不得覆盖、重排、回退或吸收到本任务逻辑。
- 新额度迁移、接口修改和测试必须基于最新工作树协调，文件冲突时先保留并行改动再适配。

## 回滚点

- 迁移前保留隔离数据库备份；迁移或旧数据转换不确定时停止，不猜测窗口。
- 存储阶段测试未通过时不进入 HTTP/UI。
- 准入或记账出现重复扣费、额度绕过、窗口提前清零时回滚本地窗口实现。
- 浏览器或全量测试未通过时不提交、不归档任务。

## 实施记录（2026-09-19）

- 新增 `006_local_quotas.sql`：保留旧月字段与账期表，新增按成员归属的本地 5h / 7d 窗口，并扩展确认作业以支持 `reset_quota_windows`。
- 当前准入与可靠费用记账仅使用本地 5h / 7d；`0` 表示不限，过期窗口在下一笔可靠费用到达时重建。
- 浏览器 API、管理表单、乘客概览、成员用量与数据保留页面已移除有效月额度投影；乘客概览改为 7 天剩余额度。
- 遗留月账期代码仅为历史查询、旧数据库兼容与旧迁移测试保留，不再由当前 HTTP / service 准入路径调用。
- 已保留并适配工作区既有的成员并发列与 API Key token 持久化改动；迁移版本使用 006。

## 质量检查记录（2026-09-19）

- 独立 `trellis-check` 已完成并修复：活跃月账期创建残留、未知费用错误启窗、DST 七自然日、迟到费用窗口覆盖、零计数窗口重置、弱类型仓储签名及跳过测试。
- 已通过：`gofmt -w .`、`git diff --check`、`node --check internal/carpool/web/assets/app.js`、`node --test test/*.test.mjs`（108+ 项，0 skip）、`go test ./internal/carpool/...`、`go test ./...`、`go vet ./...`、`go test -race ./internal/carpool/...`、普通与 `CGO_ENABLED=0` server build。
- 浏览器 fixture 的 build tag 编译通过，并已更新创建成员的 5h/7d 字段与脚本选择器；当前环境没有 Playwright/Chromium，Paseo 也没有连接浏览器自动化 host，因此桌面 1440px 与移动 390/360px 截图验收仍为 missing evidence，不能声称已完成真实视觉验收。
- 当前工作树混有此前 API Key token 持久化与成员并发列改动，尚未提交或归档；提交前必须按归属拆分或明确合并范围。


## 界面精简补充（2026-09-20）

- 管理端与乘客端所有成员/用户表格的“个人额度”单元格统一为两行紧凑视图：`5h` 进度条 + 百分比、`7d` 进度条 + 百分比。
- 表格额度单元格不再显示金额、状态、剩余、超额、重置时间和费用警告；并发仍保持独立列。
- 不限、未开始、离车或缺少有效分母时使用空轨道和 `—`，不伪造百分比；概览额度卡片保持原信息结构。
- Node 全量测试 114 项通过，0 skip；真实浏览器视觉验收仍因环境缺少 Playwright/Chromium 而为 missing evidence。
