# qiaomu-design v3.9.0 Creation Handoff

## 1. Result

- Skill: `qiaomu-design` v3.9.0
- Job: move interface work from design diagnosis and four visible directions to an explicitly selected, implemented, browser-verified result.
- Local source: canonical checked-out repository (machine-specific path omitted from the public package)
- Publication status: release candidate prepared on a feature branch; remote PR, Release, discovery, and isolated install evidence are verified by the publisher after this commit.

## 2. Reference methods and skills

- Anshu Chimala / Lenny's Newsletter: the public Technique 1–6 supplied creative seeds, ambitious prompts, user-reaction refinement, build briefs, independent screenshot critique, and media decisions. These appear in `references/creative-prompting.md` and the Phase 2/3 router.
- Sakana AI SSoT: supplied the distribution-diversity rationale for private random seed strings. It is bounded to open creative work and never used as factual evidence.
- Existing qiaomu-design lineage: previously inspected Anthropic frontend-design, Vercel Web Interface Guidelines, Emil Kowalski skills, taste-skill, Carbon, impeccable, extract-design-system, design-an-interface, and awesome-design-md mechanisms remain routed through focused references. This release did not re-benchmark their current versions.

No fresh catalog adoption or rating metrics were collected in this iteration; current popularity and comparative quality remain `missing evidence`.

## 3. Absorbed and rejected

- Keep: functional contract, design read, three dials, four visible directions, explicit selection, `DESIGN.md` anchor, two-pass browser review, preflight gate.
- Adapt: root instructions now route by task and load only relevant references; creative seeds map to concrete design axes and exclusive constraints.
- Reject: automatic K3 routing, hidden default selection, paywalled-technique speculation, generic model superiority claims, and duplicated long-form rules in `SKILL.md`.
- Invent: a single flow from seeded divergence → user reaction → build brief → selected design anchor → independent screenshot critique → subtraction.

## 4. Advantages and highlights

- `design advantage`: root `SKILL.md` is a 10.7 KB progressive-disclosure router instead of a 47.5 KB monolith, while specialist detail remains discoverable in `references/`.
- `design advantage`: external-model authorization and the Phase 2 selection boundary are explicit in both runtime instructions and public README.
- `validated advantage`: package validation, trigger evaluation, script smoke tests, PR checks, published-version checks, and isolated installation are release gates; final results are recorded only after they run.
- `hypothesis`: seeded, mutually exclusive briefs plus user-reaction refinement should increase useful visual diversity. Provider-backed A/B and new human blind-review evidence are `missing evidence`.

## 5. Verification and limits

Local package validation passed without warnings; trigger evaluation passed 14/14; six package tests passed; all Node scripts parsed; the evolution ledger verified; and an HTTP smoke test proved bridge injection, health response, selection write, sentinel output, and `--exit-on-select`. GitHub PR, Release, discovery, and clean-install evidence are still external to this source report and must be checked in the release result. The existing public case gallery is historical evidence, not proof that every future output will reach the same quality.
