# 用户偏好账本（User Preferences Ledger）

> 本文件由「自进化机制」维护：用户在使用中给出的反馈被抽象成规则后追加于此。
> **每次执行任何设计任务前必读；preflight 门禁第 0 步逐条对照本文件。**
> 与通用规则冲突时，本文件优先。

## 规则格式

```
### P-{编号} · {规则一句话}
- 级别：硬禁令 / 强偏好 / 情境规则
- 何时适用：{范围}
- 原话摘录："{用户当时的表述}"
- 收录：{日期}｜状态：生效 / 已废止（{日期，原因}）
```

---

### P-1 · 界面排版禁用斜体，中文尤其
- 级别：硬禁令
- 何时适用：一切 UI/页面产出；强调改用字重、颜色、字号
- 原话摘录："界面排版中，出现斜体会很不好看，不和谐。"
- 收录：2026-07-03｜状态：生效（已落入 SKILL.md 字体禁令 + preflight A 节）

### P-2 · 设计方向必须"先看后选"
- 级别：强偏好（已升级为招牌机制）
- 何时适用：任何涉及视觉方向的任务，生成风格试衣间预览页（4 方向 + 推荐 + 用户手动确认；禁止 60s 自动推进）
- 原话摘录："每次设计时，都快速给用户几种风格预览（用本地 web 页面弹出），用户选中方向后开始设计。"
- 收录：2026-07-03｜状态：生效（2026-07-06 更新为必须用户点选/对话明确选择，不再超时自动执行）

### P-4 · Hero 视觉侧不得简陋
- 级别：硬禁令
- 何时适用：任何分屏 Hero / 产品展示位；空心 div mockup、文字+渐变斑点均违规，按 assets-and-readability.md 三选一执行
- 原话摘录："hero 区右侧设计很简陋"
- 收录：2026-07-04｜状态：生效

### P-5 · 可读性优先于气质
- 级别：硬禁令
- 何时适用：一切含正文的页面；暗底正文 .85/次要 ≥.60、正文区纹理 ≤3%、CTA 对比度逐个审计
- 原话摘录："个人作品集可读性（差）"
- 收录：2026-07-04｜状态：生效

### P-6 · 多方向展示每个方向都要是完整世界
- 级别：强偏好
- 何时适用：正式展示多个完整设计方向时：全宽沉浸带 + 每方向独立工艺密度 ≥3 + 设计宣言入版面，禁小卡并排糊弄；Phase 2 风格试衣间按 P-35 默认桌面 2×2
- 原话摘录："多样性测试效果很差"
- 收录：2026-07-04｜状态：生效

### P-7 · 创意不许吃掉页面的功能契约
- 级别：硬禁令
- 何时适用：一切页面，开放创意命题尤其；页面类型的核心标识必须首屏可辨（404 的错误码、定价的价格、表单的提交路径）
- 原话摘录："我们 skill 做的 404 页面，为啥连 404 都没出现，虽然创意很好，但会不会有点稍微走远了一点。"
- 收录：2026-07-04｜状态：生效（已落入 SKILL.md 功能契约 + preflight A4 + 甲方评审视角）

### P-8 · 视觉方案必须交付可看的预览页
- 级别：硬禁令
- 何时适用：用户要求"出方案 / 几个方向 / 重构方案 / 设计方案 / UI 方案 / 参考方案"且涉及视觉方向选择时
- 原话摘录："那你为什么不遵守skill要求，出相对推荐方案的设计HTML，让我看一眼"
- 收录：2026-07-04｜状态：生效（已落入 SKILL.md Phase 2 方案输出验收门 + preflight A5；2026-07-05 更新为浅层 `design-previews/YYYY-MM-DD-任务名/index.html`）

### P-9 · 设计预览选择必须回传到当前工作流
- 级别：硬禁令
- 何时适用：任何视觉方向预览页、方案选择页、风格试衣间；用户可见文案只说"已回传"，不绑定具体调用端
- 原话摘录："做好 Preview 后应该起一个 web 服务，自动打开网页让用户预览，用户的选择动作会回传通知当前工作流，知道选了哪个选项。"
- 收录：2026-07-04｜状态：生效（2026-07-05 更新为调用端无关表述；已落入 SKILL.md 本地预览回传桥 + style-preview.md + preflight A5）

### P-10 · 设计预览选择外壳必须一致且按钮明显
- 级别：硬禁令
- 何时适用：任何设计方向预览页；demo 内部可变化，外侧选择框架不许每次换交互心智
- 原话摘录："这个选择页面有明显的选择按钮，而且选择Demo外侧的包裹界面是不是应该风格保持一致，避免每次用户都需要理解下"
- 收录：2026-07-04｜状态：生效（已落入 style-preview.md 固定选择外壳 + preflight A5）

### P-11 · 预览回传必须在同一回合被消费
- 级别：硬禁令
- 何时适用：任何本地预览回传桥；启动预览后必须保持 watcher/轮询，不能只给 URL 就结束当前工作流
- 原话摘录："但是我点了，为什么没有回传继续执行啊"
- 收录：2026-07-04｜状态：生效（已落入 SKILL.md 本地预览回传桥 + style-preview.md + preflight A5）

### P-12 · 设计 Skill 不默认注入乔木 Profile
- 级别：硬禁令
- 何时适用：qiaomu-design 生成的预览页、最终页面、组件和设计稿；除用户明确要求，禁止默认添加打赏、公众号、GitHub/X 浮条、乔木推荐入口等个人站点 affordance
- 原话摘录："乔木设计 skill需要优化，只是设计，并不需要添加了乔木的profile等信息（打赏、公众号等）"
- 收录：2026-07-05｜状态：生效

### P-13 · 预览产物目录必须浅层可发现
- 级别：硬禁令
- 何时适用：Phase 2 风格试衣间、多方向 HTML 预览、临时设计方向文件；默认写入当前项目根目录 `design-previews/YYYY-MM-DD-任务名/index.html`，同目录放 `selection.json` 和必要 assets；用户可指定输出目录；不在项目中时退到 `~/Desktop/qiaomu-design-YYYY-MM-DD-任务名/`
- 原话摘录：".qiaomu-design/previews/<timestamp> 这种路径用户反而不好找吧……不需要那么多层级嵌套，只需要能按日期和任务区分每次任务"
- 收录：2026-07-05｜状态：生效

### P-3 · 装饰性中文 webfont 严禁滥用
- 级别：硬禁令
- 何时适用：一切含中文的页面；正文/UI 系统栈，装饰字体仅创意标题且 text= 子集化
- 原话摘录："用户大概率没有装这些字体，如果用 cdn 会有速度影响。只有个别场景可以用，大多数场景千万别滥用。"
- 收录：2026-07-03｜状态：生效（已落入 chinese-typography.md 第一硬规则 + preflight D2）

### P-14 · 常规加载与成功状态不得常驻占版面
- 级别：硬禁令
- 何时适用：正式对外 UI；除错误、空态、阻塞中的操作或用户刚触发的短 toast 外，不展示"已载入..."、"正在读取..."这类系统自言自语
- 原话摘录："低俗小说 已载入。 已载入 24/ 250部 IMDb Top 250 电影。 类似这种文案完全没必要还占地方"
- 收录：2026-07-04｜状态：生效

### P-15 · 顶部导航必须融入页面场景
- 级别：强偏好
- 何时适用：正式网站和产品页面；顶部 bar、背景、首屏内容块应共享空间层级、材质、圆角和阴影，不要像后贴的浏览器工具条
- 原话摘录："顶部bar和背景融合也不好。顶部bar和下面的内容融合真的不够好"
- 收录：2026-07-04｜状态：生效

### P-16 · 设计拨盘必须用户可懂且可调
- 级别：硬禁令
- 何时适用：设计读取、风格试衣间、方案说明、最终回复里展示 VARIANCE / MOTION / DENSITY 时
- 原话摘录："VARIANCE 8、MOTION 6、DENSITY 5 这是啥意思，担心用户看不懂，代码里不用变，但给用户展示时要用中文讲明白。用户应该也可以调整吧？"
- 收录：2026-07-05｜状态：生效

### P-17 · 自发现设计问题必须自动沉淀
- 级别：硬禁令
- 何时适用：设计任务执行、截图验收、预览回传、lint/test/build、真实交互验证中，执行代理自己发现可复现问题时
- 原话摘录："还有刚才你出错了，是否需要迭代优化到skill中，未来你自己发现的所有问题都自动写入设计记忆，然后迭代skill"
- 收录：2026-07-05｜状态：生效

### P-18 · 预览服务不得重复注入选择按钮
- 级别：硬禁令
- 何时适用：Phase 2 风格试衣间、本地预览回传服务、任何带方向选择按钮的 HTML 预览
- 原话摘录："流程跑通了，不错，但有一个……还有刚才你出错了"
- 收录：2026-07-05｜状态：生效（本次自发现问题：生成页已有选择按钮，预览服务又注入一层，导致双按钮；已落入 style-preview.md + preflight A5）

### P-19 · 预览页必须说明只是设计方向样机
- 级别：硬禁令
- 何时适用：Phase 2 风格试衣间、多方向 HTML 预览、任何用于让用户选择视觉方向的页面
- 原话摘录："你生成的只是设计方向，是不是要在预览页跟用户说明啊？"
- 收录：2026-07-05｜状态：生效（已落入 SKILL.md Phase 2 + style-preview.md + preflight A5）

### P-20 · 多个方向默认用 Subagent 或独立 brief 隔离开发
- 级别：强偏好
- 何时适用：生成多个设计方向、风格试衣间、方案对比页；能调用 subagent 时每个方向一个 subagent，不能时按独立 brief + scoped CSS 降级
- 原话摘录："还有多个方向你是不是应该调用Subagent开发，然后嵌入预览页，避免互相干扰啊。"
- 收录：2026-07-05｜状态：生效（已落入 SKILL.md Phase 2 + style-preview.md + preflight A5）

### P-21 · 选择按钮去重必须按服务注入后的真实 DOM 验证
- 级别：硬禁令
- 何时适用：Phase 2 风格试衣间、本地预览回传服务、任何自带 `.qmdp-pick-button` 的方向预览页
- 原话摘录："还有多个方向你是不是应该调用Subagent开发，然后嵌入预览页，避免互相干扰啊。"（本次自发现：按钮嵌在 `.meta` 内时，服务旧逻辑仍重复注入）
- 收录：2026-07-05｜状态：生效（已修复 preview server 识别逻辑，并落入 style-preview.md + preflight A5）

### P-22 · 设计样机和方向解释必须分区展示
- 级别：硬禁令
- 何时适用：Phase 2 风格试衣间、多方向预览页、任何让用户先看方向再选择的 HTML
- 原话摘录："设计和解释应该分开吧，混在一起用户看不懂"
- 收录：2026-07-05｜状态：生效（已落入 SKILL.md Phase 2 + style-preview.md + preflight A5）

### P-23 · 预览服务重启后旧端口不再作为验收依据
- 级别：硬禁令
- 何时适用：本地预览回传服务、截图验收、给用户发送预览 URL
- 原话摘录："http://127.0.0.1:57372/ 为啥还是有两层选择按钮"
- 收录：2026-07-05｜状态：生效（重启服务必须告知新 URL；旧端口/旧页面视为过期缓存）

### P-24 · 页面自带按钮和服务注入按钮必须职责分离
- 级别：硬禁令
- 何时适用：本地预览回传服务、任何自带 `.qmdp-pick-button` 与选择回传脚本的预览页
- 原话摘录："为啥还是有两层选择按钮"（本次自发现：页面自带按钮已有 handler 时，服务全局点击监听仍可能重复处理同一次选择）
- 收录：2026-07-05｜状态：生效（服务自动注入按钮必须带 `data-qmdp-injected` 或等价标识；非服务注入按钮交给页面脚本处理）

### P-25 · 多方向预览的移动端 mockup 也必须可读
- 级别：硬禁令
- 何时适用：Phase 2 风格试衣间、多方向 HTML 预览、任何含 mini mockup 的方向选择页
- 原话摘录："设计和解释应该分开吧，混在一起用户看不懂"（本次自发现：移动端页面无横向滚动，但复杂 mockup 内部文字和控件仍可能互相挤压）
- 收录：2026-07-05｜状态：生效（移动端截图必须覆盖；复杂方向用内部缩放舞台、移动端专用布局或可控裁切）

### P-26 · 预览页遵守奥卡姆剃刀
- 级别：硬禁令
- 何时适用：Phase 2 风格试衣间、多方向预览页、方向选择外壳；只保留帮助用户选择的必要信息，删掉重复说明、小字教程、长段解释和系统自言自语
- 原话摘录："预览页面要用奥卡姆剃刀原则，不要添加没必要的说明文案"
- 收录：2026-07-05｜状态：生效

### P-27 · 选择按钮已有文案时不再叠加说明
- 级别：硬禁令
- 何时适用：任何方向选择页；按钮已写清"选择 A/B/C/D · 方向名"时，顶部或卡片上方不再重复"点按钮选择"之类说明
- 原话摘录："按钮已经有文案了，上面是不是不用加小字说明了"
- 收录：2026-07-05｜状态：生效

### P-28 · 预览样机必须填满或居中
- 级别：硬禁令
- 何时适用：多方向 mini mockup、嵌套预览舞台、方向卡片；样机应占满留给它的视觉空间，比例不匹配时居中，不允许出现大片无意义空白
- 原话摘录："预览中的不同设计页面应该占满留给的空间吧，不要留出空白区间，要么你就居中"
- 收录：2026-07-05｜状态：生效

### P-29 · 用户可见回传文案不绑定具体工具
- 级别：硬禁令
- 何时适用：预览页 toast、顶部状态、降级提示、交付话术；skill 可能运行在不同代理环境，统一说"已回传"、"当前工作流"，不写具体工具名
- 原话摘录："skill 可能不止用于某一个工具，你只需要说已回传，不要说回传到具体工具。"
- 收录：2026-07-05｜状态：生效

### P-30 · 选择检测要接近即时
- 级别：硬禁令
- 何时适用：本地预览回传桥、选择 watcher、方向选择页；用户确认后必须立即 POST，监听端优先使用文件事件、服务日志哨兵或 `--exit-on-select` 快速路径，短轮询只做兜底，默认轮询间隔不超过 75ms
- 原话摘录："点击选择到检测到选中内容，等待的时间太久了，有什么优化空间？"
- 收录：2026-07-05｜状态：生效（2026-07-06 再次强化：确认后回传仍慢，必须缩短观测链路）

### P-31 · 电商详情页必须先守住购物心智模型，再谈风格
- 级别：硬禁令
- 何时适用：电商详情页（PDP）、商品购买页、转化型购物页、含 SKU 与加购流程的页面
- 原话摘录："我觉得都不满意，不像正常的电商网站"
- 收录：2026-07-05｜状态：生效
- 教训：把"高端旗舰 + 暗色专业户外"理解成「军规装备手册 / 户外杂志封面 / 工业产品手册 / 科技仪表盘」这类强概念方向，违反了电商页的功能契约。电商详情页有强心智模型——主图轮播 + 价格区（含划线价/促销）+ 规格SKU选择 + 数量 + 立即购买/加入购物车 + 服务保障 + 卖点模块 + 详情长图 + 参数表 + 评价 + 固定加购底栏。**结构骨架不许被风格概念替换**；风格只能落到配色、字体、材质、留白节奏上。Phase 2 出方向时，"正常电商结构 + 高端化视觉"优先于"概念化品牌页"；视觉冒险度拨盘对电商页默认压到 3-4，因为它是功能页不是营销页

### P-32 · README 视频案例优先用 GIF 内联展示
- 级别：强偏好
- 何时适用：GitHub README、案例展示页、skill 对外说明文档需要展示视频案例时
- 原话摘录："你可以把视频转gif放上去啊！"
- 收录：2026-07-06｜状态：生效
- 教训：GitHub README 对仓库内 MP4 的播放器渲染不稳定；用户要“看得到能动”的案例时，优先把 MP4 转成体积可控的 GIF 直接内联展示，MP4 只作为源文件或备用链接保留，不再用静态封面图代替视频

### P-33 · 方向选择必须二次确认，但确认后的回传路径要短
- 级别：硬禁令
- 何时适用：风格试衣间、方向选择页、本地预览回传桥
- 原话摘录："预览的界面中，是不是应该支持三个拨盘的滑动选项调整，还有选中方向后出现弹层，让用户确认，然后加一个输入框，用户可以输入调整建议，确定后一起回传处理？" / "用户点击后回传还是很慢，你想想办法"
- 收录：2026-07-06｜状态：生效
- 教训：点方向或键盘 1-4 只能打开确认弹层，弹层必须带三拨盘值和调整建议输入框；点击“确认并回传”后立刻 POST，并通过服务日志哨兵、文件事件或 `--exit-on-select` 让执行流程尽快观测到选择

### P-34 · 页面自带选择按钮默认仍由预览服务接管
- 级别：硬禁令
- 何时适用：本地预览回传服务、任何带 `.qmdp-pick-button` 的预览页
- 原话摘录："那我们测试下？用glm写一个向阳乔木个人主页"（本次自发现：GLM 按规范生成了 `.qmdp-pick-button` 但未绑定 handler；服务旧逻辑把页面自带按钮一律跳过，导致可见按钮点击无效）
- 收录：2026-07-06｜状态：生效
- 教训：`.qmdp-pick-button` 只是“有明显按钮”的结构标识，不等于页面已经自管回传。未标记的页面自带按钮默认由服务接管并打开确认弹层；只有按钮显式标记 `data-qmdp-native-handler="true"` 或 `data-qmdp-managed="page"` 时，服务才跳过它，避免双回传

### P-35 · 风格试衣间默认 2×2，拨盘数值必须实时更新
- 级别：硬禁令
- 何时适用：Phase 2 风格试衣间、多方向 HTML 预览、本地预览回传服务
- 原话摘录："生成的预览页面中，滑块滑动没有显示数值变化" / "预览页面的四种样式，是不是还是左右两栏显示更好？"
- 收录：2026-07-06｜状态：生效
- 教训：四方向预览在桌面端默认应是 2×2 左右两栏网格，让用户横向比较而不是长滚动；移动端再收敛为单列。三拨盘的数值显示必须随滑块 input 事件即时更新，不能只在最终回传里带正确值

### P-36 · 预览卡片按钮上方说明不得挤成竖排
- 级别：硬禁令
- 何时适用：Phase 2 风格试衣间、多方向 HTML 预览、任何方向卡片内的说明 / 标签 / 选择按钮区域
- 原话摘录："预览界面按钮上面的介绍文本显示有异常"
- 收录：2026-07-06｜状态：生效
- 教训：方向说明被放进极窄 side rail、metric column 或 grid auto column 时，会变成一字一行并和选择按钮抢空间。方向卡片内只保留方向名、极短标签和选择按钮；若必须保留按钮上方短说明，必须使用 full-width 横排 meta block，桌面实际宽度至少 240px / 18em，最多两行。长解释移到独立方向说明区，选择按钮独立成行且不得覆盖说明文字

### P-37 · 设计方向阶段必须交付四方向预览
- 级别：硬禁令
- 何时适用：qiaomu-design 的 Phase 2、任何重设计 / 设计方案 / 给方向 / 视觉方向选择任务；尤其是用户追问为什么没有预览时
- 原话摘录："写入记忆，一定要做四种方向设计和预览，skill要优化。"
- 收录：2026-07-08｜状态：生效
- 教训：Phase 2 不能退化成文字诊断或三套方案列表；必须交付 4 个轴级差异的真实 mini mockup、`design-previews/YYYY-MM-DD-任务名/index.html`、明确推荐方向、三拨盘、确认弹层和本地回传服务。若上一轮只给了文字，下一轮必须直接补预览，不再解释流程或要求用户重新确认

### P-38 · 禁止用左侧竖线装饰选中态或卡片强调
- 级别：硬禁令
- 何时适用：一切产品 UI / 列表项 / 卡片 / Case 选中态 / 导航项；包括 `border-left` 色条、`box-shadow: inset Npx 0` 竖条、伪元素左侧竖线
- 规则：选中与强调改用「整块底色 + 全边框 / 字重 / 轻微阴影」，禁止任何「卡片或列表项左侧竖线装饰」
- 原话摘录："我不喜欢任何卡片左侧出现竖线装饰，你修改网站并写入到乔木设计skill中"
- 收录：2026-07-09｜状态：生效

### P-39 · 并列工作区必须满高并隔离滚动链
- 级别：硬禁令
- 何时适用：桌面端侧栏 + 主内容、双栏编辑器、文件树 + 阅读区、列表 + 详情等并列可滚动工作区
- 规则：固定侧栏在主内容滚动时必须保持视口内并占满可用高度；左右区域各自滚动，使用明确的可收缩高度与 `overscroll-behavior: contain` 防止滚到底后把滚动传给相邻区域或页面；移动端改为单一主滚动区，避免多层滚动
- 原话摘录："无论页面如何上下滑动，左侧栏应该一直要占满屏幕高度吧。左右两侧都有滚动条时，左侧和右侧的滚动条控制不应该冲突"
- 收录：2026-07-11｜状态：生效（已落入 engineering-checklist.md + preflight D）

### P-40 · 执行乔木设计 Skill 必须真实调用 Kimi K3
- 级别：硬禁令
- 何时适用：`qiaomu-design` 的诊断、Phase 2、Phase 3、打磨与发布前视觉审稿
- 规则：必须通过 `qiaomu-model-cli` 真实调用本机 `kimi-code/k3`，并让 K3 看到本次真实代码、页面或截图；禁止只在提示词或交付文案中声称“K3 设计”。完整重设计至少有实现前与实现后两次 K3 证据，诊断任务至少一次；交付前保存 provider、实际 model、CLI 版本、exit code、elapsed、session id 或 response SHA-256、输入产物和原始/归一化输出路径，并关闭最高优先级问题
- 原话摘录："乔木设计skill执行也要用k3"
- 收录：2026-07-26｜状态：已废止（2026-09-01，被 P-46“仅当前任务明确点名 K3 才调用”覆盖）

### P-41 · 阅读器首屏正文必须早于目录与全书索引
- 级别：硬禁令
- 何时适用：电子书、文档、长文阅读器的打开流程与加载态
- 规则：正文首屏、翻页与当前位置恢复是主工作流，目录、全书进度、搜索索引和统计必须异步加载，任何一个失败或变慢都不得阻塞第一屏可读内容；加载态用保持版式的 skeleton，不用空白页
- 原话摘录："为什么 loading 目录这么慢，你计划如何优化"
- 收录：2026-08-03｜状态：生效（已落入 assets-and-readability.md + preflight A4.3）

### P-42 · 技术图片文件名不能冒充图注
- 级别：硬禁令
- 何时适用：EPUB、文档、富文本、相册等由文件名或 OCR 元数据生成的图片标签
- 规则：`IMG_256`、`DSC_1234.jpg`、`Screenshot_2026...` 等纯技术文件名必须从可见图注和无意义 title/alt 中清除；只匹配窄而明确的文件名模式，作者写的自然语言图注与有语义的替代文本必须保留
- 原话摘录："图片解析下面的图片名不要显示，比如 IMG_256 等"
- 收录：2026-08-03｜状态：生效（已落入 assets-and-readability.md + preflight A4.3）

### P-43 · 单栏与双栏必须是精确可见页数
- 级别：硬禁令
- 何时适用：EPUB、PDF、文档等分页阅读器的单页/双页、单栏/双栏布局
- 规则：标注单栏时视口只能出现 1 个完整阅读页，标注双栏时只能出现左右 2 个完整阅读页；不得露出第三栏、半栏或相邻页。验收必须在超宽屏、侧栏开关、全屏和位置恢复后检查真实可见几何，不能只断言 `columnWidth` 变小或设置值已保存
- 原话摘录："为什么显示的不是两栏或单栏？"
- 收录：2026-08-03｜状态：生效（已落入 assets-and-readability.md + preflight A4.3）

### P-44 · 阅读方式与页面布局必须分离
- 级别：硬禁令
- 何时适用：EPUB、PDF、文档和长文阅读器
- 规则：阅读方式单独提供“上下滚动 / 左右翻页”；上下滚动固定为连续单栏，左右翻页才允许单栏/双栏。正文图片默认约束在当前正文栏内、保持原比例且不强制放大，不自动跨越双栏。沉浸阅读页不常驻占高顶栏，进度归入目录导航语境，低频工具在右上角通过悬停、点击、触摸或键盘聚焦渐进显露
- 原话摘录："可以，按这个执行。另外书中的图片应该按多少宽度显示呢？是不是要用设定的宽度或通栏？还有顶部的bar是不是可以去掉，阅读进度在目录中的表调下面显示？右侧的功能放在右上角，hover或点击这块区域时才显示？"
- 收录：2026-08-03｜状态：生效（已落入 assets-and-readability.md + preflight A4.3）

### P-45 · 所有需要修改真实前端文件的设计任务，Kimi K3 CLI 是主设计与直接实现者：必须在隔离的项目工作区读取现有代码并修改实际文件；执行代理负责需求约束、差异审查、测试与浏览器验收，不得把 K3 降级为只给建议或只做审稿。
- 级别：硬禁令
- 何时适用：qiaomu-design 的 Web 前端 UI、CSS、组件、页面重设计与打磨任务
- 规则：所有需要修改真实前端文件的设计任务，Kimi K3 CLI 是主设计与直接实现者：必须在隔离的项目工作区读取现有代码并修改实际文件；执行代理负责需求约束、差异审查、测试与浏览器验收，不得把 K3 降级为只给建议或只做审稿。
- 原话摘录："前端设计直接用kimi k3 cli设计，修改我们的规范"
- 收录：2026-08-22｜状态：已废止（2026-09-01，被 P-46 覆盖）
- 证据：evt-1cdf60d254

### P-46 · 默认由当前 Codex 执行；K3 仅在当前任务明确点名时调用
- 级别：硬禁令
- 何时适用：设计、前端、UI、CSS、组件、页面与 qiaomu-design 全流程
- 规则：默认由当前 Codex 执行；K3 仅在当前任务明确点名时调用
- 原话摘录："2026-09-01 纠正摘要：设计、前端、UI、CSS、组件和页面任务默认由当前 Codex 直接完成；只有当前任务明确点名 K3 才调用。"
- 收录：2026-09-04｜状态：生效
- 证据：用户在 2026-09-01 的明确纠正（机器私有记忆路径不进入公共包）

### P-47 · 开放式方向用创意提示词发散，并以用户反应、独立截图批评和删减通道收敛
- 级别：强偏好
- 何时适用：开放式设计、多方向探索和已有方向仍显模板化时；不适用于像素复刻、修 bug、事实任务或单一正确答案
- 规则：开放式方向用创意提示词发散，并以用户反应、独立截图批评和删减通道收敛
- 原话摘录："读取这篇文章中的提示词技巧，优化我们的乔木设计skill"
- 收录：2026-09-04｜状态：生效
- 证据：https://www.lennysnewsletter.com/p/how-to-turn-your-ai-into-a-world, https://pub.sakana.ai/ssot/
