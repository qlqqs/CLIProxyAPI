# 拼车计费最终交付说明（2026-09-14）

## 项目目标

在原有 CPA 代理之上提供隔离的拼车工作台：管理员管理乘客、车辆、账号和月度美元
限额；乘客查看同车用量、原生配额并管理个人 API Key。计费复用实际上游用量，
未知数据明确标记，不猜算历史费用，也不改变旧全局 Key 的行为。

## 本轮交付

- 请求授权时冻结只读价格目录，执行校验、同步计费、延迟异步回退共用同一版本。
  中途更新目录只影响后续新请求；目录及 tier 深拷贝防止调用方意外修改。
- 在实际模型解析与插件改写后、调用上游前检查价格；缺价返回 HTTP `422` 和
  `model_price_not_configured`，不增加上游调用、不惩罚账号、不扩大车辆范围。
- 补齐 HTTP handler 的拼车上下文继承，保留 validator、同步用量观察器及冻结授权，
  不覆盖调用方取消语义。Claude 保持原协议错误外壳，仅为可信本地错误保留安全 code。
- 原生配额缺少百分比时只显示状态；真实零仍为零。进度条使用原生 `progress`，
  避免 CSP 禁止行内样式后所有比例误显示满格；未放宽 CSP。
- 修正本期重置与结束账期清理错误复用“明细保留截止时间”的问题：账期操作使用当前
  时刻，90/180/365 天仅影响明细过期判断；避免显示成功但当前金额没有归零。
- 隔离浏览器验收脚本与 opt-in Go fixture，可重复操作真实页面、真实 HTTP、临时 SQLite
  及假上游；不读取生产配置、真实账号或连接外部上游。

## 兼容与边界

- 本轮不新增数据库迁移、outbox、租约树、全局容量系统或崩溃后全局启动封锁。
- 正常用量同步落账；运行中写库失败拒绝新生成请求，待补写缓存满时等待数据库恢复。
  崩溃期间不承诺精确费用恢复；历史 `incomplete` 明确保留，但不封锁整个服务。
- 原有 Key、模型读取与允许路由范围保持不变。密码重置使旧网页登录会话失效；
  强制改密门禁针对工作台，不删除车辆、账单或 API Key。
- 浏览器上游为可核对的合成数据，不代表真实供应商/OAuth、Firefox/Safari、生产部署验收。
- 旧父/子 PRD 的未勾选要求不自动视为本轮全数完成；例如完整筛选组合、预览版本绑定、
  重复确认幂等和全部历史升级矩阵，不以本轮浏览器截图替代专项证据。

## 验证与已知问题

最终结果与复现命令见当前任务的 `research/browser-acceptance.md` 和 `research/delivery-check.md`。
扩大 race 首轮出现一次未归属拼车的 WebSocket 关闭码测试间歇 EOF：期望 `1012`，实际
`1006`。没有 `DATA RACE` 报告；该用例 `-race -count=3` 及完整 OpenAI handler 包重跑
通过。既有 `TryLock` 关闭路径可能产生此症状，但未证明根因，不能宣称已修复。

## 发布操作

本次仅准备本地交付，不自动推送、部署或修改生产数据库。部署前按既有流程备份 SQLite
与配置，使用正常构建替换服务并保留回滚版本；不要用本浏览器 fixture 启动生产服务。
